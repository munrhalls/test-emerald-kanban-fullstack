/**
 * Tracer-bullet integration point.
 *
 * Milestone 1 placeholder: returns a hardcoded string so the page renders
 * end to end. A follow-up task will replace the body with a live Supabase
 * query without changing this signature or its call site in `app/page.tsx`.
 */
export async function getTracerValue(): Promise<string> {
  return "TRACER-OK · emerald conduit standing by";
}
